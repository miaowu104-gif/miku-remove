# nothing generated should ever land in the repo
node_modules/
*.log

# downloaded song cache lives outside the project, but just in case
audio/*.mp3
audio/*.flac
audio/*.wav
audio/*.m4a
audio/*.ogg
audio/*.opus
audio/*.aac

# installer build artefacts - both are reproducible from Setup.cs with
# installer/build-installer.ps1, and the exe is published as a release asset
installer/payload.zip
installer/*.exe

# OS noise
Thumbs.db
desktop.ini
.DS_Store
