# nothing generated should ever land in the repo
node_modules/
*.log

# downloaded song cache lives outside the project, but just in case
audio/*.mp3
audio/*.flac
audio/*.wav
audio/*.m4a
audio/*.ogg
audio/*.opus
audio/*.aac

# installer build artefacts - all reproducible with installer/msi/build-msi.ps1
installer/payload/
installer/*.msi
installer/*.wixpdb
installer/*.exe

# source material kept next to the repo but never shipped:
# the reference video, the violin cover, and the notes they came from
_source/

# OS noise
Thumbs.db
desktop.ini
.DS_Store
